name: omk
version: 1.1.0
description: OMK orchestration preset. Generates spec-kit artifacts optimized for OMK DAG execution, evidence gates, worktree isolation, and multi-agent parallel pipelines.
author: open-multi-agent-kit
priority: 100
templates:
  spec: templates/spec-template.md
  plan: templates/plan-template.md
  tasks: templates/tasks-template.md
commands:
  plan: commands/plan.md
  tasks: commands/tasks.md
